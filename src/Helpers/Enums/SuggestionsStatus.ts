export enum SuggestionsStatus {
    'Any string' = 0,
    'Strings without suggestions' = 1,
    'Strings with suggestions awaiting approval' = 2,
    'Strings with suggestions awaiting review' = 3,
    'Strings with approved suggestions awaiting review' = 4
}
